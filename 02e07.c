#include <stdio.h>

int main(void) {
    int main = 5;
    int If = 4;
    int printf = 3;
    //int for = 2;
    //int while = 3;
    return 0;
}


//for and while are keywords, but if one uses printf as a variable name, printf function may not be used. 